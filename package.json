{
  "name": "api-curriculo",
  "version": "1.0.0",
  "description": "API REST para gerenciar currículos",
  "main": "api/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node api/index.js",
    "dev": "nodemon api/index.js",
    "prisma:seed": "node prisma/seed.js",
    "prisma:generate": "npx prisma generate",
    "postinstall": "npx prisma generate",
    "vercel-build": "npx prisma migrate deploy && npx prisma generate"
  },
  "keywords": ["api", "rest", "curriculo", "express", "prisma", "postgresql"],
  "author": "Elias Pereira da Silva Junior",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.9.0",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "prisma": "^6.9.0",
    "nodemon": "^3.1.0"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  }
}